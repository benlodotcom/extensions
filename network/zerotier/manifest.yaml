version: v1alpha1
metadata:
  name: zerotier
  version: "$VERSION"
  author: Benjamin
  description: |
    A smart ethernet switch for the earth.
  compatibility:
    talos:
      version: ">= v1.5.0"
